# NIS Strategy Builder — MVP dependencies
streamlit>=1.36
anthropic>=0.39
pydantic>=2.6
python-dotenv>=1.0
pycountry>=23.12

# Document extraction
pymupdf>=1.24            # PDF (fitz)
pdfplumber>=0.11         # PDF fallback
python-docx>=1.1         # Word read + export
openpyxl>=3.1            # Excel read + export
python-pptx>=0.6         # PowerPoint read + export

# Exports
reportlab>=4.1           # PDF export

# Durable cloud storage (optional — Supabase REST)
requests>=2.31

# Optional (alternative PDF engine)
# weasyprint>=62
