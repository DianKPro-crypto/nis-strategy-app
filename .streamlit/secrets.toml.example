# Streamlit Cloud → App settings → Secrets. Copy these lines and fill in your key.
# (Locally you can instead use a .env file.)

ANTHROPIC_API_KEY = "sk-ant-..."
ANTHROPIC_MODEL = "claude-opus-4-8"

# Optional — durable cloud storage (Supabase). See SUPABASE_SETUP.md
SUPABASE_URL = "https://xxxxxxxx.supabase.co"
SUPABASE_KEY = "eyJ...the anon public key..."
